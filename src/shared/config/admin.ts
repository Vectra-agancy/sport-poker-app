/**
 * Временный режим открытой админки: каждый авторизованный пользователь
 * считается админом и может смотреть/редактировать данные в /admin.
 * Перед боевым запуском поставить false — права снова будут браться
 * из поля User.isAdmin в БД.
 */
export const EVERYONE_IS_ADMIN = true;
